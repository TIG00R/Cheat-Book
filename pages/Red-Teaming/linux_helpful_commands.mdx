### Services
```bash copy filename="Bash"
# List running services
systemctl --type=service --state=running
```


### SSH
```bash copy filename="Bash"
# Generatet SSH key pair
ssh-keygen -t ecdsa
ssh-keygen -t rsa
```